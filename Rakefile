#!/usr/bin/env rake
require "bundler/gem_tasks"

# require 'rake/testtask'

# Rake::TestTask.new do |t|
  # puts 'voila'
  # # t.libs << 'lib/elastic_model'
  # # t.test_files = FileList['spec/lib/elastic_model/*_spec.rb']
  # # t.verbose = true
# end

# task :default => :test
